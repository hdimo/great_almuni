import 'package:flutter/material.dart';

class MessagerieHomePage extends StatelessWidget {
  const MessagerieHomePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Messagerie'),
    );
  }
}
