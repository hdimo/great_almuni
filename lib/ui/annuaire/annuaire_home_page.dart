import 'package:flutter/material.dart';

class AnnuaireHomePage extends StatelessWidget {
  const AnnuaireHomePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Annuaire'),
    );
  }
}
